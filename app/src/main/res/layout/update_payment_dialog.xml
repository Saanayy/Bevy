<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="200dp"
    android:layout_height="200dp"
    android:layout_margin="16dp"
    android:padding="16dp">

    <TextView
        android:id="@+id/dialog_payment_placeholder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:text="Amount" />

    <EditText
        android:id="@+id/dialog_payment_amount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:layout_below="@+id/dialog_payment_placeholder"
        android:layout_alignStart="@+id/dialog_payment_placeholder" />

    <EditText
        android:id="@+id/dialog_payment_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:focusable="false"
        android:layout_below="@+id/dialog_payment_amount"
        android:hint="Date" />

</RelativeLayout>